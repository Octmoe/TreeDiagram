/**
 * M1 占位入口：M2 里程碑将实现完整的 Fastify 服务
 * （路由 / 鉴权 / 错误映射，见 docs/API_CONTRACT.md 与 IMPLEMENTATION_DESIGN §11）。
 */
export function main(): void {
  console.error('TreeDiagram server 将在 M2 里程碑提供。');
  process.exitCode = 1;
}

main();
