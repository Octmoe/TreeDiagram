/**
 * M1 占位页面：M2 里程碑将实现完整的四区编辑器 UI
 * （树 + 搜索 / Inspector / Workflow 面板 / ChangeSet 抽屉）。
 */
export function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>TreeDiagram</h1>
      <p>M1 状态内核已就绪；编辑器 UI 将在 M2 里程碑交付。</p>
    </main>
  );
}
